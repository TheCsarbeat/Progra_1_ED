#include "main_thread.h"
/*
machines_thread::machines_thread(){

}
void machines_thread::__init__(ArrayMachines * array, QMutex *mutex) {
    this->array = array;
    this->running = false;
    this->paused = false;
    this->mutex = mutex;
}

void machines_thread::run() {
    this->mutex->unlock();
    this->running = true;
    while (running) {
        while (paused) {
            sleep(1);
        }

    }
    this->mutex->lock();
}

void machines_thread::pause() {
    this->paused = true;
}

void machines_thread::stop() {
    this->running = false;
}

void machines_thread::resume() {
    this->paused = false;
}*/
